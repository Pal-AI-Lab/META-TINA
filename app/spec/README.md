# Spec Directory

This directory holds the full text of TINA Spec 0.1 (TINA-SPEC-0.1.md).

It is the canonical text: when executing M3 (verbatim copy of the
protocol invariants) during assembly, this is the sole source.

Except for a spec version upgrade under maintenance mode, the contents of
this directory must never be modified.
