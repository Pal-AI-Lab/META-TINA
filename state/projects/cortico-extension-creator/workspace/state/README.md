# state 区

运行时的记忆与工作台,全部可写:

- `JOURNAL.md` 日志,格式在文件里;
- `design/` 每个包一份设计记录,`<包名>.md`,一条决定一段,写理由;
- `cortico/` Cortico 的 clone,不进本工作区的 git;
- `packages/` 做出来的扩展包,每个是自己的 git 仓库,不进本工作区的 git。

开发者随时可以看这里的任何文件;这些本来就是他的东西。
